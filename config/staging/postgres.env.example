# Disposable PostgreSQL test DB (OR-02). Never use production DB names.
# Requires: docker run -d --name erp-pytest-pg -e POSTGRES_DB=erp_pytest \
#   -e POSTGRES_HOST_AUTH_METHOD=trust -p 5432:5432 postgres:16-alpine

ERP_TEST_POSTGRES_URL=postgresql+psycopg://postgres@localhost/erp_pytest
