"""Shell layout constants — single breakpoint contract (Phase A stabilization)."""

# Viewport: mobile/tablet uses bottom nav + keyed header; desktop uses sidebar.
SHELL_MOBILE_MAX_PX = 968
SHELL_DESKTOP_MIN_PX = SHELL_MOBILE_MAX_PX + 1  # 969
SHELL_SIDEBAR_WIDTH_PX = 244
